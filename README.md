# Next.js Firebase + Stripe Starter


1. Copy files to a new repo
2. Create a Firebase project and enable Email/Password auth
3. Create a Stripe account and get API keys
4. Fill `.env.local` from `.env.local.template`
5. `npm install`
6. `npm run dev`
7. Deploy to Vercel and set env vars in Vercel project settings
